apiVersion: vmoperator.vmware.com/v1alpha3
kind: VirtualMachine
metadata:
  name: oc-mysql
  labels:
    vm-selector: oc-mysql
    vm-lb-opencart: vm-lb-selector
    topology.kubernetes.io/zone: z-wld-a
spec:
  className: best-effort-small
  # Friendly image name: noble-server-cloudimg-amd64. Your vmi- ID is pod-unique —
  # run "kubectl get vmi" from the vcfa:dev-xxxxx context and replace the value below
  imageName: vmi-58bfe5816baf49fa0
  storageClass: cluster-wld01-01a-storage-policy
  powerState: PoweredOn
  network:
    hostName: oc-mysql
    domainName: vcf.lab
    nameservers:
      - 8.8.8.8
  bootstrap:
    cloudInit:
      rawCloudConfig:
        name: oc-mysql-bootstrap-secret
        key: user-data
