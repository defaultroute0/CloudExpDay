apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
#  namespace: dev-xxx
spec:
  selector:
    app: nginx
  type: LoadBalancer
  ports:
    - name: http-80
      port: 80
      targetPort: 80
      protocol: TCP
