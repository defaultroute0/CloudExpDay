apiVersion: v1
kind: Service
metadata:
  name: postgres-lb
#  namespace: dev-bnnj5
spec:
  selector:
    app: postgres
  type: LoadBalancer
  ports:
    - name: tcp-5432
      port: 5432
      targetPort: 5432
      protocol: TCP
